services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      # Tambahkan environment variables lainnya di Coolify
    restart: unless-stopped
