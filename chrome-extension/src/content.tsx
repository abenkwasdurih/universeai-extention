import { initAntiF12 } from './anti-f12';

// 1. Initialize Anti-F12 protections immediately on page load
initAntiF12();

// UI has been removed as per the new design.
// The content script now only serves to inject Anti-F12 protections.
